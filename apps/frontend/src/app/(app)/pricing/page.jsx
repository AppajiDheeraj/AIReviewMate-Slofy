import SimplePricing from "@/components/mvpblocks/simple-pricing";

/**
 * Render the pricing page using the SimplePricing block.
 *
 * @returns {JSX.Element} A JSX element that renders the SimplePricing component.
 */
export default function PricingPage() {
    return <SimplePricing/>;
}